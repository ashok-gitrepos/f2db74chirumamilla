extends layout

block content
  h1= title
  - var image_names = ["basic", "CAP", "ironmaster", "rouge", "strength"]
  - for(let i = 0; i < 3; i++) {
    - let imageNo = Math.floor(Math.random() * 5)
        img(src=`/images/${image_names[imageNo]}.jpg`, alt="", height="300px", width="300px")
  - }